# Finance Bot

Отдельный Telegram-бот для личного финансового учета на Python, aiogram 3.x и SQLite.

## Что умеет

- записывает доходы и расходы в SQLite;
- открывается как Telegram Mini App с мобильной формой учета бюджета;
- ведет подписки и присылает напоминания об оплате;
- хранит месячные лимиты по категориям и предупреждает при 80% и 100%;
- строит HTML-отчеты за день, неделю и месяц;
- принимает простые русские фразы без строгих команд;
- распознает текст со скриншотов чеков и банковских операций через OCR;
- хранит денежные значения в целых копейках и мигрирует старые SQLite-схемы.

## Структура

```text
finance_bot/
  bot.py
  db.py
  money.py
  parser.py
  reminders.py
  report.py
  services.py
  webapp.py
  webapp_static/
    index.html
    styles.css
    app.js
  requirements.txt
```

## Настройка

```bash
python -m venv .venv
source .venv/bin/activate
pip install -r finance_bot/requirements.txt
```

Для распознавания скриншотов нужен системный Tesseract OCR и языковые пакеты русского/английского. Например, в Debian/Ubuntu:

```bash
sudo apt-get install tesseract-ocr tesseract-ocr-rus tesseract-ocr-eng
```

Обязательные переменные:

```bash
export TELEGRAM_BOT_TOKEN="telegram_bot_token"
export ALLOWED_USER_IDS="123456789"
```

Опциональные переменные:

```bash
export FINANCE_DB_PATH="$HOME/finance_bot/finance.db"
export FINANCE_REPORT_DIR="$HOME/finance_bot/reports"
export FINANCE_BOT_TIMEZONE="Asia/Novosibirsk"
```

Если `ALLOWED_USER_IDS` пустой, бот никого не пустит.

## Telegram Mini App

Mini App включается, когда задан публичный HTTPS-адрес веб-приложения:

```bash
export TELEGRAM_WEB_APP_URL="https://example.com"
export FINANCE_WEBAPP_HOST="0.0.0.0"
export FINANCE_WEBAPP_PORT="8080"
```

После этого команда `/start` покажет кнопку **Открыть бюджет**. Внутри Mini App можно:

- посмотреть доходы, расходы и остаток за текущий месяц;
- добавить доход или расход;
- сохранить лимит на категорию;
- добавить подписку;
- отметить подписку оплаченной.

API Mini App проверяет `initData` Telegram WebApp и дополнительно сверяет пользователя с `ALLOWED_USER_IDS`. Для локальной разработки можно открыть `/health`, а сам Mini App должен быть доступен Telegram по HTTPS-домену, указанному в `TELEGRAM_WEB_APP_URL`.

## Запуск

```bash
python -m finance_bot.bot
```

## Фразы

```text
потратил 450 на такси
заплатил 450 за такси
пришла зп 100000
заработал 100000
добавь подписку YouTube 299 дата 12.03.2026
добавь подписку YouTube 1 299,50 завтра
лимит на такси 5000
отчет за день
отчет за неделю
отчет за месяц
/balance
баланс
/last
последние операции
/paid 3
оплатил подписку YouTube
```

Также можно прислать скриншот чека или банковской операции как фото либо PNG/JPG/WEBP-файл. Бот распознает текст, попробует найти сумму по словам `итого`, `сумма`, `оплата`, `покупка`, `списание`, `к оплате` или `всего` и запишет расход.

Если сумма не распознана, бот попросит уточнить. Поддерживаются суммы с пробелами и запятой: `1 299,50`. Если категория не указана, используется `прочее`.

Команда `/balance` или фраза `баланс` показывает доходы, расходы и остаток за всё время. Команда `/last` или фраза `последние операции` выводит последние 10 операций прямо в чате.

## База данных

По умолчанию база создается здесь:

```text
~/finance_bot/finance.db
```

Таблицы:

- `transactions`
- `subscriptions`
- `budgets`

Схема версионируется через `PRAGMA user_version`; денежные поля пишутся в `*_cents`, а старые `REAL`-значения мигрируются при запуске.

Все изменения пишутся в SQLite реально, без имитации.

## Отчеты

HTML-отчет содержит:

- баланс за период;
- доходы, расходы и остаток;
- таблицу операций;
- активные подписки;
- круговую диаграмму расходов по категориям;
- график расходов по дням.

Отчет автономный: CSS и изображения встроены в HTML, внешних ресурсов нет.
